# IMVSBlobFindModu 参数说明

> 帮助文档：`GUID-F5B9D37D-90C6-45DE-8880-DADBE017C0CC.html`

| 参数名 | 类型 | 默认值 | 帮助章节 |
|--------|------|--------|----------|
| BlobNumLimitHigh | Integer | 99999 | - |
| BlobNumLimitLow | Integer | 1 | - |
| FindNum | Integer | 100 | - |
| HightSoftThreshold | Integer | 0x96 | - |
| HightThreshold | Integer | 0x96 | - |
| HoleMinArea | Integer | 0 | - |
| LowSoftThreshold | Integer | 0x64 | - |
| LowThreshold | Integer | 0x64 | - |
| MaxArea | Integer | 999999999 | - |
| MaxAreaInnerRectNum | Integer | 0x0 | 高级参数 |
| MaxBoxAngle | Integer | 90 | - |
| MaxLongAxis | Integer | 999999999 | - |
| MaxOutPixelNum | Integer | 0 | - |
| MaxPerimeter | Integer | 999999999 | - |
| MaxRectHeight | Integer | 200 | - |
| MaxRectWidth | Integer | 200 | - |
| MaxShortAxis | Integer | 999999999 | - |
| MinArea | Integer | 10 | - |
| MinBoxAngle | Integer | -90 | - |
| MinLongAxis | Integer | 10 | - |
| MinPerimeter | Integer | 10 | - |
| MinRectHeight | Integer | 100 | - |
| MinRectWidth | Integer | 100 | - |
| MinShortAxis | Integer | 1 | - |
| OverlapRatio | Integer | 0 | - |
| SoftHighRatio | Integer | 60 | 阈值方式 |
| SoftLeftRatio | Integer | 5 | - |
| SoftLowRatio | Integer | 40 | 阈值方式 |
| Softness | Integer | 254 | - |
| SoftRightRatio | Integer | 5 | 阈值方式 |
| AngleLimitEnable | Boolean | False | - |
| AxisRatioEnable | Boolean | False | 高级参数 |
| BinaryImageEnable | Boolean | True | - |
| BlobAreaLimitEnable | Boolean | False | - |
| BlobAreaProportionLimitEnable | Boolean | False | - |
| BlobNumLimitEnable | Boolean | False | - |
| BlobTotalAreaLimitEnable | Boolean | False | - |
| BlobTotalAreaProportionLimitEnable | Boolean | False | - |
| BolbImageEnable | Boolean | True | - |
| BoxHeightLimitEnable | Boolean | False | - |
| BoxWidthLimitEnable | Boolean | False | - |
| CenterXLimitEnable | Boolean | False | - |
| CenterYLimitEnable | Boolean | False | - |
| CentroidXLimitEnable | Boolean | False | - |
| CentroidYLimitEnable | Boolean | False | - |
| CircularityLimitEnable | Boolean | False | - |
| LongAxisLimitEnable | Boolean | False | - |
| MaxOutPixelNumEnable | Boolean | False | 高级参数 |
| OKWhenNumIsZero | Boolean | False | - |
| PerimeterLimitEnable | Boolean | False | - |
| RectangularityLimitEnable | Boolean | False | - |
| ScoreLimitEnable | Boolean | False | - |
| SelectByArea | Boolean | True | - |
| SelectByBoxAngle | Boolean | False | 高级参数 |
| SelectByCentraBias | Boolean | False | 高级参数 |
| SelectByCircularuty | Boolean | False | 高级参数 |
| SelectByHistInfo | Boolean | False | - |
| SelectByLongAxis | Boolean | False | 高级参数 |
| SelectByPerimeter | Boolean | False | 高级参数 |
| SelectByRectangularity | Boolean | False | 高级参数 |
| SelectByRectHeight | Boolean | False | - |
| SelectByRectWidth | Boolean | False | - |
| SelectByShortAxis | Boolean | False | 高级参数 |
| ShortAxisLimitEnable | Boolean | False | - |

---

## 阈值方式

定义图像二值化的阈值设置方式，包括不进行二值化、单阈值、双阈值、自动阈值等。图像二值化的结果将直接影响Blob分析结果，请根据实际场景谨慎设置阈值方式。
适用于输入图像本身已为二值图的场景。
通过单阈值算法进行图像二值化，需设置低阈值。该算法适用于对算法效率要求较高或需对较大检测目标进行二值化的场景。该算法二值化处理速度快，但存在空间量化误差，相对精度较低。
不同极性设置下，二值化效果有所差异。极性用于指定在二值化之前，灰度图中前景的灰度是否亮于背景。
极性设置为暗于背景时，灰度值小于低阈值的像素点被分割为前景，其余像素点为背景。
极性设置为亮于背景时，灰度值大于或等于低阈值的像素点被分割为前景，其余像素点为背景。
通过双阈值算法进行图像二值化。需设置两个阈值（低阈值和高阈值）。该算法适用于对算法效率要求较高或需对较大检测目标进行二值化的场景。该方式二值化处理速度快，但存在空间量化误差，相对精度较低。
当低阈值小于高阈值时，灰度值介于两个阈值之间的像素点将被分割为前景，其余像素点为背景。
当低阈值大于高阈值时，灰度值在两个阈值之外的像素点将被分割为前景，其余像素点为背景。
算法根据图像自动配置阈值进行图像二值化，适用于需要根据输入图像动态计算阈值的场景。不同极性设置下，二值化效果有所差异。极性用于指定在二值化之前，灰度图中前景的灰度是否亮于背景。
软阈值算法是相对于硬阈值算法的概念，分为固定软阈值算法和相对软阈值算法。软阈值算法可降低由空间量化误差（Spatial

Quantization Error，SQE）导致的Blob大小、轮廓长度和位置的不精确问题。
硬阈值算法，即上文已提及的单阈值、双阈值和自动阈值。
下图展示了硬阈值算法与软阈值算法的核心区别。为了降低空间量化误差对算法输出结果的影响，软阈值算法给位于感兴趣区域内的每个像素点分配一个像素权重。像素权重为0，表明该像素是一个背景像素；像素权重为1，表明该像素是一个前景像素；像素权重在0和1之间，表明该像素位于前景和背景区域的边界。
软阈值（固定）即固定软阈值算法，适用于目标和背景之间可进行简单二值划分的场景。该算法耗时相对较长。
通过该算法进行图像二值化，需设置阈值范围和软阈值柔和度。不同极性设置下，二值化效果有所差异。极性用于指定在二值化之前，灰度图中前景的灰度是否亮于背景。
极性为暗于背景时，灰度值小于等于低阈值的像素权重为1（为前景）；大于等于高阈值的像素权重为0（为背景）；介于两阈值之间的像素权重在0和1之间，即为前景和背景区域的边界。
极性为亮于背景时，灰度值小于等于低阈值的像素点的权重为0（为背景）；大于等于高阈值的像素点的权重为1（为前景）；介于两阈值之间的像素权重在0和1之间，即为前景和背景区域的边界。
设置像素权重值0和1之间的阶梯个数（如下图所示）。该值为0时，软阈值处理结果等价于硬阈值。
柔和度越高，则Blob边缘越柔和，毛刺感越弱（如下图所示）。
通过相对软阈值算法进行图像二值化。该二值化方法适用于照明环境不稳定的场景。该算法耗时相对较长。
选用该算法，需设置低尾部比例、高尾部比例、低阈值比例、高阈值比例和上文已提及的软阈值柔和度。
相对软阈值算法的工作流主要包括两个步骤。以下结合上图对该两个步骤进行说明。
通过高/低尾部比例排除具有极端灰度值的若干像素点。如果图像的像素总数为N，高/低尾部比例分别为H%和L%，则图像中灰度值最低的N×L%个像素和灰度值最高的N*H%个像素分别赋予0和1的权重值。
在剩余像素点的灰度值范围内划分高/低阈值。如果剩余像素点的灰度值分布在[A,

B]的范围内，低阈值比例为TL%，高阈值比例为TH%，则低阈值为（B-A）* TL% +

A，高阈值为（B-A）* TH% + A。
极性为亮于背景时，灰度值小于等于低阈值的像素权重为0（为背景），大于等于高阈值的像素权重为1（为前景），介于两阈值之间的像素权重在0和1之间。
极性为暗于背景时，灰度值小于等于低阈值的像素权重为1（为前景），大于等于高阈值的像素权重为0（为背景），介于两阈值之间的像素权重在0和1之间。
起始于0灰度值的较低灰度值像素点相对于像素点总数的比例。这些较低灰度值像素点将在图像二值化中忽略。
终止于255灰度值的较高灰度值像素点相对于像素点总数的比例。该部分像素点将在图像二值化中忽略。
去除低尾部比例和高尾部比例对应的像素点后，灰度值低于低阈值的像素点相对所有剩余像素点的比例。
去除低尾部比例和高尾部比例对应的像素点后，灰度值在最低灰度值至高阈值之间的像素点相对所有剩余像素点的比例。

### 不进行二值化

适用于输入图像本身已为二值图的场景。

### 单阈值

关联参数：`低阈值`、`极性`、`极性`、`极性`、`暗于背景`、`低阈值`、`极性`、`亮于背景`、`低阈值`

通过单阈值算法进行图像二值化，需设置低阈值。该算法适用于对算法效率要求较高或需对较大检测目标进行二值化的场景。该算法二值化处理速度快，但存在空间量化误差，相对精度较低。
不同极性设置下，二值化效果有所差异。极性用于指定在二值化之前，灰度图中前景的灰度是否亮于背景。
极性设置为暗于背景时，灰度值小于低阈值的像素点被分割为前景，其余像素点为背景。
极性设置为亮于背景时，灰度值大于或等于低阈值的像素点被分割为前景，其余像素点为背景。
图 5 原图
图 6 单阈值且极性为暗于背景
图 7 单阈值极性为亮于背景

### 双阈值

关联参数：`低阈值`、`高阈值`、`低阈值`、`高阈值`、`低阈值`、`高阈值`

通过双阈值算法进行图像二值化。需设置两个阈值（低阈值和高阈值）。该算法适用于对算法效率要求较高或需对较大检测目标进行二值化的场景。该方式二值化处理速度快，但存在空间量化误差，相对精度较低。
当低阈值小于高阈值时，灰度值介于两个阈值之间的像素点将被分割为前景，其余像素点为背景。
当低阈值大于高阈值时，灰度值在两个阈值之外的像素点将被分割为前景，其余像素点为背景。

### 自动阈值

关联参数：`极性`、`极性`

算法根据图像自动配置阈值进行图像二值化，适用于需要根据输入图像动态计算阈值的场景。不同极性设置下，二值化效果有所差异。极性用于指定在二值化之前，灰度图中前景的灰度是否亮于背景。

### 软阈值（固定） → `SoftThreshold_Fixed`

关联参数：`单阈值`、`双阈值`、`自动阈值`、`软阈值（固定）`、`阈值范围`、`软阈值柔和度`、`极性`、`极性`、`极性`、`暗于背景`、`极性`、`亮于背景`、`硬阈值`

软阈值算法是相对于硬阈值算法的概念，分为固定软阈值算法和相对软阈值算法。软阈值算法可降低由空间量化误差（Spatial
Quantization Error，SQE）导致的Blob大小、轮廓长度和位置的不精确问题。
说明：
硬阈值算法，即上文已提及的单阈值、双阈值和自动阈值。
下图展示了硬阈值算法与软阈值算法的核心区别。为了降低空间量化误差对算法输出结果的影响，软阈值算法给位于感兴趣区域内的每个像素点分配一个像素权重。像素权重为0，表明该像素是一个背景像素；像素权重为1，表明该像素是一个前景像素；像素权重在0和1之间，表明该像素位于前景和背景区域的边界。
图 8 软阈值和硬阈值的区别
软阈值（固定）即固定软阈值算法，适用于目标和背景之间可进行简单二值划分的场景。该算法耗时相对较长。
通过该算法进行图像二值化，需设置阈值范围和软阈值柔和度。不同极性设置下，二值化效果有所差异。极性用于指定在二值化之前，灰度图中前景的灰度是否亮于背景。
极性为暗于背景时，灰度值小于等于低阈值的像素权重为1（为前景）；大于等于高阈值的像素权重为0（为背景）；介于两阈值之间的像素权重在0和1之间，即为前景和背景区域的边界。
极性为亮于背景时，灰度值小于等于低阈值的像素点的权重为0（为背景）；大于等于高阈值的像素点的权重为1（为前景）；介于两阈值之间的像素权重在0和1之间，即为前景和背景区域的边界。
软阈值柔和度
设置像素权重值0和1之间的阶梯个数（如下图所示）。该值为0时，软阈值处理结果等价于硬阈值。
图 9 柔和度原理
柔和度越高，则Blob边缘越柔和，毛刺感越弱（如下图所示）。
图 10 柔和度比对效果

### 软阈值（相对） → `SoftThreshold_Relative`

关联参数：`低尾部比例`、`高尾部比例`、`低阈值比例`、`高阈值比例`、`软阈值柔和度`、`高/低尾部比例`、`高/低尾部比例`、`低阈值比例`、`高阈值比例`、`极性`、`亮于背景`、`极性`、`暗于背景`

通过相对软阈值算法进行图像二值化。该二值化方法适用于照明环境不稳定的场景。该算法耗时相对较长。
选用该算法，需设置低尾部比例、高尾部比例、低阈值比例、高阈值比例和上文已提及的软阈值柔和度。
图 11 相对软阈值算法原理
相对软阈值算法的工作流主要包括两个步骤。以下结合上图对该两个步骤进行说明。
通过高/低尾部比例排除具有极端灰度值的若干像素点。如果图像的像素总数为N，高/低尾部比例分别为H%和L%，则图像中灰度值最低的N×L%个像素和灰度值最高的N*H%个像素分别赋予0和1的权重值。
在剩余像素点的灰度值范围内划分高/低阈值。如果剩余像素点的灰度值分布在[A,
B]的范围内，低阈值比例为TL%，高阈值比例为TH%，则低阈值为（B-A）* TL% +
A，高阈值为（B-A）* TH% + A。
极性为亮于背景时，灰度值小于等于低阈值的像素权重为0（为背景），大于等于高阈值的像素权重为1（为前景），介于两阈值之间的像素权重在0和1之间。
极性为暗于背景时，灰度值小于等于低阈值的像素权重为1（为前景），大于等于高阈值的像素权重为0（为背景），介于两阈值之间的像素权重在0和1之间。
低尾部比例
起始于0灰度值的较低灰度值像素点相对于像素点总数的比例。这些较低灰度值像素点将在图像二值化中忽略。

### 高尾部比例 → `SoftRightRatio`

终止于255灰度值的较高灰度值像素点相对于像素点总数的比例。该部分像素点将在图像二值化中忽略。

### 低阈值比例 → `SoftLowRatio`

关联参数：`低尾部比例`、`高尾部比例`

去除低尾部比例和高尾部比例对应的像素点后，灰度值低于低阈值的像素点相对所有剩余像素点的比例。

### 高阈值比例 → `SoftHighRatio`

关联参数：`低尾部比例`、`高尾部比例`

去除低尾部比例和高尾部比例对应的像素点后，灰度值在最低灰度值至高阈值之间的像素点相对所有剩余像素点的比例。

## 查找个数

设置最终输出的Blob的最大数量。

## 填充面积阈值

Blob区域内的最小非Blob区域面积（可理解为是Blob中的“孔洞”）。如果“孔洞”面积小于或等于该值，则将其与外围的Blob合并。
孔洞填充前
孔洞填充后

## 面积使能

开启后，算法仅筛选出面积在面积范围内的Blob。

## 轮廓输出使能

开启后，图像上的Blob将显示轮廓。

## Blob图像输出

关闭后，算法将不输出Blob分析后的图像。

## 二值化图输出

关闭后，算法将不输出Blob分析后的二值化图像。

## 高级参数

开启后，可输出亮度分布直方图信息。
开启后，算法仅筛选出角度在角度范围内的Blob。此处的角度指Blob的“最小外接矩形”的长轴与图像x轴方向的夹角（顺时针为正，逆时针为负的）。
开启后，算法仅筛选出Blob矩形框宽度在RECT宽范围内的Blob。
此处的“矩形框”与最小外接矩形不同，特指宽高均与图像宽高平行的矩形（如下图所示）。
开启后，算法仅筛选出矩形框高度在RECT高范围内的Blob。
此处的“矩形框”与最小外接矩形不同，特指宽高均与图像宽高平行的矩形。
开启后，算法仅筛选出轮廓长度在周长范围内的Blob。
开启后，算法仅筛选出最小外接矩形短轴长在短轴范围内的Blob。
开启后，算法仅筛选出最小外接矩形长轴长度在长轴范围内的Blob。
开启后，算法仅筛选出圆形度在圆形度范围内的Blob。
该计算公式中，circularity为圆形度，S为Blob面积（即连通域面积），d为Blob质心到Blob轮廓点的最大距离。圆形度范围为0~1，圆的圆形度为1。圆形度越小，Blob形状越不规则。
质心为Blob内的所有像素点的坐标平均值所对应的点。
开启后，算法仅筛选出矩形度在矩形度范围内的Blob。
该计算公式中， R为矩形度， S1为Blob面积（即连通域面积）， S2为Blob的最小外接矩形的面积。矩形度范围是0~1，矩形的矩形度为1。矩形度越小，Blob形状越不规则。
开启后，算法仅筛选出质心偏移在质心偏移范围内的Blob。
质心为Blob内的所有像素点的坐标平均值所对应的点，如下图中蓝点所示；矩形中心是Blob的最小外接矩形的几何中心，如下图中红点所示。两点之间的距离即为质心相对于矩形中心的偏移量。
开启后，算法仅筛选出最小外接矩形短轴长度和长轴长度比值在轴比范围内的Blob。
设置输出的Blob在当前结果页签内的排序依据。可依据面积、周长、圆形度、矩形度、连通域中心x、连通域中心y、box角度、box宽、box高、矩形左上顶点x、矩形左上顶点y、二阶中心距主轴角度、轴比等特征排序。
实际配置效果与下文的排序方式相关。
按升序或降序对输出的Blob在在当前结果页签内进行排序。也可设置为不排序。
设置判断图像上像素点是否互相连通的标准，包括4连通和8连通。通常设置为8连通可比设置为4连通获得更大的Blob。
以下示例中，如果采用4连通定义“连通”，则下图白色像素点构成的特征图像包含两个“连通区域”，即如下左图中两个橙框中的由白色像素点构成的区域；如果采用8连通定义，则仅包含一个“连通区域”，即如下右图中所有由白色像素点构成的区域。
可设置计算多少个Blob的最大内接矩形。
开启后，如果Blob中与图像边缘、ROI边缘或掩膜边缘重合的像素点数量大于最大超界像素，那么该Blob将被算法删除。
如果将最大超界像素设置为0，所有与图像边缘、ROI边缘或掩膜边缘相交的Blob都会被删除。

### 直方图信息输出 → `LumHistogramEnable`

开启后，可输出亮度分布直方图信息。

### 角度使能 → `SelectByBoxAngle`

关联参数：`角度范围`

开启后，算法仅筛选出角度在角度范围内的Blob。此处的角度指Blob的“最小外接矩形”的长轴与图像x轴方向的夹角（顺时针为正，逆时针为负的）。
图 12 最小外接矩形

### RECT宽使能

关联参数：`RECT宽范围`

开启后，算法仅筛选出Blob矩形框宽度在RECT宽范围内的Blob。
说明：
此处的“矩形框”与最小外接矩形不同，特指宽高均与图像宽高平行的矩形（如下图所示）。
图 13 Blob矩形框

### RECT高使能

关联参数：`RECT高范围`

开启后，算法仅筛选出矩形框高度在RECT高范围内的Blob。
说明：
此处的“矩形框”与最小外接矩形不同，特指宽高均与图像宽高平行的矩形。

### 周长使能 → `SelectByPerimeter`

关联参数：`周长范围`

开启后，算法仅筛选出轮廓长度在周长范围内的Blob。

### 短轴使能 → `SelectByShortAxis`

关联参数：`短轴范围`

开启后，算法仅筛选出最小外接矩形短轴长在短轴范围内的Blob。

### 长轴使能 → `SelectByLongAxis`

关联参数：`长轴范围`

开启后，算法仅筛选出最小外接矩形长轴长度在长轴范围内的Blob。

### 圆形度使能 → `SelectByCircularuty`

关联参数：`圆形度范围`

开启后，算法仅筛选出圆形度在圆形度范围内的Blob。
图 14 圆形度计算公式
该计算公式中，circularity为圆形度，S为Blob面积（即连通域面积），d为Blob质心到Blob轮廓点的最大距离。圆形度范围为0~1，圆的圆形度为1。圆形度越小，Blob形状越不规则。
说明：
质心为Blob内的所有像素点的坐标平均值所对应的点。
图 15 圆形度

### 矩形度使能 → `SelectByRectangularity`

关联参数：`矩形度范围`

开启后，算法仅筛选出矩形度在矩形度范围内的Blob。
图 16 矩形度计算公式
该计算公式中， R为矩形度， S1为Blob面积（即连通域面积）， S2为Blob的最小外接矩形的面积。矩形度范围是0~1，矩形的矩形度为1。矩形度越小，Blob形状越不规则。

### 质心偏移使能 → `SelectByCentraBias`

关联参数：`质心偏移范围`

开启后，算法仅筛选出质心偏移在质心偏移范围内的Blob。
质心为Blob内的所有像素点的坐标平均值所对应的点，如下图中蓝点所示；矩形中心是Blob的最小外接矩形的几何中心，如下图中红点所示。两点之间的距离即为质心相对于矩形中心的偏移量。
图 17 质心偏移

### 轴比范围 → `AxisRatioEnable`

关联参数：`轴比范围`

开启后，算法仅筛选出最小外接矩形短轴长度和长轴长度比值在轴比范围内的Blob。

### 排序特征

关联参数：`排序方式`

设置输出的Blob在当前结果页签内的排序依据。可依据面积、周长、圆形度、矩形度、连通域中心x、连通域中心y、box角度、box宽、box高、矩形左上顶点x、矩形左上顶点y、二阶中心距主轴角度、轴比等特征排序。
实际配置效果与下文的排序方式相关。

### 排序方式

关联参数：`不排序`

按升序或降序对输出的Blob在在当前结果页签内进行排序。也可设置为不排序。

### 连通性

关联参数：`4连通`、`8连通`、`8连通`、`4连通`、`4连通`、`8连通`

设置判断图像上像素点是否互相连通的标准，包括4连通和8连通。通常设置为8连通可比设置为4连通获得更大的Blob。
以下示例中，如果采用4连通定义“连通”，则下图白色像素点构成的特征图像包含两个“连通区域”，即如下左图中两个橙框中的由白色像素点构成的区域；如果采用8连通定义，则仅包含一个“连通区域”，即如下右图中所有由白色像素点构成的区域。
图 18 两个连通区域
图 19 单个连通区域

### 最大内接矩形输出数量 → `MaxAreaInnerRectNum`

可设置计算多少个Blob的最大内接矩形。

### 像素超界使能 → `MaxOutPixelNumEnable`

关联参数：`最大超界像素`、`最大超界像素`

开启后，如果Blob中与图像边缘、ROI边缘或掩膜边缘重合的像素点数量大于最大超界像素，那么该Blob将被算法删除。
如果将最大超界像素设置为0，所有与图像边缘、ROI边缘或掩膜边缘相交的Blob都会被删除。
图 20 像素超界

